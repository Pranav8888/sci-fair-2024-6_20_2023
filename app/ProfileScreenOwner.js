import {TouchableOpacity, TextInput, View, Text, Image} from "react-native";
import { useRouter } from "expo-router";
const ProfileScreenOwner = () => {
    return (
        <View>
            <Text> 
               Home Screen
            </Text>
        </View>
    ) 
}

export default ProfileScreenOwner;